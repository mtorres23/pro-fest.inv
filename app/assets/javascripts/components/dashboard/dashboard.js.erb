angular
  .module('fifoApp')
  .component('dashboard', {
    templateUrl: '<%= asset_path("components/dashboard/dashboard") %>',
    controller: DashboardController
  });

DashboardController.$inject = [];

function DashboardController() {
  var vm = this;

  vm.message = 'Ayo Bitch!';
}

